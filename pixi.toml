[workspace]
authors = ["Maxim Zaks <maxim.zaks@gmail.com>"]
channels = ["https://conda.modular.com/max", "conda-forge"]
name = "dagr-shared-buffers-ipc-mojo-odin-example"
platforms = ["osx-arm64"]
version = "0.1.0"

[tasks]
# Run the full cross-process demo: Mojo GPU producer + Odin raylib renderer,
# sharing one mmap'd Dagr SharedBuffer.
demo = "./run_demo.sh"

[dependencies]
mojo = "==1.0.0"
max = "==26.5.0"
